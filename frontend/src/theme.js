import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        mode: 'dark',
        primary: {
          main: '#5e0a00',
        },
        secondary: {
          main: '#008b8b',
        },
        divider: '#8b0000',
      }
})

  export default theme;